export const logoCss = `
/* Nexus logo block */
.brand,
.brand-mark {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  min-width: max-content;
  white-space: nowrap;
  flex-shrink: 0;
}

.brand-logo-mark {
  width: 42px;
  height: 42px;
  min-width: 42px;
  flex: 0 0 42px;
  border-radius: 999px;
  display: grid;
  place-items: center;
  background: #111827;
  color: #ffffff;
  font-family: Georgia, serif;
  font-size: 0.85rem;
  font-weight: 700;
  line-height: 1;
}

.brand-name {
  display: inline-block;
  color: inherit;
  font-weight: 700;
  white-space: nowrap;
}`;
